# Build outputs
build/
.gradle/
*.class

# Secrets — NEVER commit these
secrets.properties
.env

# SSL private keys — never commit
certs/*.key
certs/*.p12
certs/*.srl

# Bundled bot credentials (generated into build dir, never committed)
launcher.properties

# Node
pres-assistant-addin/node_modules/
pres-assistant-addin/dist/

# Generated addin static resources (built by webpack, bundled into JAR)
src/main/resources/static/

# Claude Code
.claude/

# IDE
.idea/
*.iml
*.iws
*.ipr

# OS
.DS_Store
Thumbs.db